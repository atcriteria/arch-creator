// Takes form data from the application and wraps tags around the data.

const formatArchFile= () => {
    console.log("formatting arch")
    const formattedArchFile = "";
    return formattedArchFile;
}

export default formatArchFile;